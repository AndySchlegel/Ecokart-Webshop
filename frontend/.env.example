# ============================================================================
# API CONFIGURATION
# ============================================================================
# Backend API Base URL
# Development: http://localhost:4000
# Production: https://api.your-domain.com
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000

# ============================================================================
# AWS COGNITO CONFIGURATION
# ============================================================================
# Diese Werte bekommst du aus Terraform Output oder AWS Console

# Cognito User Pool ID (z.B. "eu-north-1_AbCdEfG")
# Findest du in: AWS Console → Cognito → User Pools
# Oder: terraform output -json | jq '.cognito_user_pool_id.value'
NEXT_PUBLIC_USER_POOL_ID=

# Cognito User Pool Client ID (z.B. "1a2b3c4d5e6f7g8h9i0j")
# Findest du in: AWS Console → Cognito → User Pool → App Integration → App clients
# Oder: terraform output -json | jq '.cognito_client_id.value'
NEXT_PUBLIC_USER_POOL_CLIENT_ID=

# AWS Region (z.B. "eu-north-1")
# Sollte gleich sein wie deine Terraform Region
NEXT_PUBLIC_AWS_REGION=eu-north-1
